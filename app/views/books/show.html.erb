title : <%= @book.title %></br>
page : <%= @book.page %></br>
price : <%= @book.price %></br>
description : <%= @book.description %></br>

<%= link_to 'List of book', books_path %> 

<% if flash[:notice].present? %>
<%= flash[:notice] %>
<% end %>

<%= render 'info', popular_book: 'Attack On Titan' %>